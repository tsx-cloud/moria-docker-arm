services:
  moria:
    image: andrewsav/moria
    container_name: moria
    restart: unless-stopped
    stop_signal: SIGINT
    volumes:
      - './server:/server'
      - './steam:/root/Steam'
    ports:
      - '7777:7777/udp'
